--download image
docker pull apache/nifi

--run default image
docker run --name nifi -p 9090:9090 -d -e NIFI_WEB_HTTP_PORT='9090' apache/nifi:latest

--run with volume
docker run \
    --name nifi \
    -p 9090:9090 \
    -d \
    -v /home/aarmentah/nifiSource/jms/lib/:/home/nifi/jms/lib \
    -v /home/aarmentah/nifiSource/flowfile/:/home/nifi/flowfile \
    -e NIFI_WEB_HTTP_PORT='9090' \
    apache/nifi:latest
